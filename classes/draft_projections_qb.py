class DraftProjectionsQB:

	def __init__(self, playerId, completions, attempts, passingYards, passingTds, passingInt, rushAtt, rushYards, rushTd, fumbles, fantasyPoints, displayName, team):
		self.playerId = playerId
		self.completions = completions
		self.attempts = attempts
		self.passingYards = passingYards
		self.passingTds = passingTds
		self.passingInt = passingInt
		self.rushAtt = rushAtt
		self.rushYards = rushYards
		self.rushTd = rushTd
		self.fumbles = fumbles
		self.fantasyPoints = fantasyPoints
		self.displayName = displayName
		self.team = team
	